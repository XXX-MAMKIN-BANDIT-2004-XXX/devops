#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <sstream>
class BubbleSort
{
	BubbleSort();

public:
	static void sort(std::vector<int>& arr);

	static void vectorDisplay(std::vector<int> arr);
};

